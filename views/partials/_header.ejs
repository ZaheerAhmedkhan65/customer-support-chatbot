<nav class="navbar navbar-expand-lg bg-white py-3 position-sticky top-0 z-1">
    <div class="container">
        <a data-link class="navbar-brand fw-bold" href="/">
            Support<span class="text-muted">Bot</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if(page==='index' ) {%>
                <li class="nav-item">
                    <a data-link class="nav-link" href="/#features">Features</a>
                </li>
                <li class="nav-item">
                    <a data-link class="nav-link" href="/#pricing">Pricing</a>
                </li>
                <% } %>
            </ul>
            <div class="d-flex align-items-center">
                <% if (typeof user !== 'undefined' && user) { %>
                    <a data-link href="/dashboard" class="btn btn-outline-primary me-4">Dashboard</a>
                    <div class="btn-group dropdown">
                        <button type="button" class="btn btn-outline-primary rounded-circle" data-bs-toggle="dropdown" data-bs-display="static"
                            aria-expanded="false">
                            <%= user.email[0].toUpperCase() %>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-lg-end">
                            <li>
                                <a href="/auth/logout" onclick="return confirm('Are you sure you want to logout?');" class="dropdown-item text-danger">Logout</a>
                            </li>
                        </ul>
                    </div>
                <% } else { %>
                    <a data-link href="/auth/signin" class="btn btn-outline-primary me-2">Sign In</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>